# ML notes

Training command: `npm run ml:indian` (synthetic Indian profiles).

**Metrics are indicative only.** They are not calibrated on real bureau populations. The structured + text blend (documented as ~85/15) is a heuristic, not a proven causal feature.

See [`ml/MODEL_CARD.md`](../ml/MODEL_CARD.md) for dataset, metrics, and limitations.
