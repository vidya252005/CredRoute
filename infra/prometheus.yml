global:
  scrape_interval: 15s

scrape_configs:
  - job_name: credroute-api
    static_configs:
      - targets: ["host.docker.internal:5000"]
    metrics_path: /api/metrics
    params:
      format: [prometheus]
